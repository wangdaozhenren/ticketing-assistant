---
name: ticketing-assistant
slug: ticketing-assistant
displayName: 演出票务助手
description: 演出票务比价助手——同场次演出票，防黄牛防假票。当用户想比演出票价格、演唱会票、音乐节票、话剧票、大麦猫眼哪个便宜、ticket price comparison 时使用。覆盖：大麦、猫眼、官方票务平台（待接入渠道）。场次票价分区对比（内场/看台），官方渠道优先、非官方风险提示，退换规则与转让限制说明。需演出名称/城市/场次信息。只做比价，不代购票、不代付款。
summary: "演出票务比价助手——同场次演出票，防黄牛防假票。当用户想比演出票价格、演唱会票、音乐节票、话剧票、大麦猫眼哪个便宜、ti…"
version: 1.1.1
license: MIT
tags: 演出票比价,演唱会,音乐节,话剧,大麦,猫眼,票价分区,内场,看台,防黄牛,假票,ticketing-comparison,演出票务助手
---

# 演出票务助手

同一场次演出，比**官方渠道票价**，防黄牛防假票。

## 工作流

1. **解析需求**：提取 `演出名`、`城市`、`场次/日期`、`人数`、可选 `票价区间`、`座位偏好`（内场/看台）。缺演出名或城市时先问一次。

2. **平台路由**：默认大麦 → 猫眼 → 官方票务（保利/摩天轮等按演出类型）；用户点名平台时以用户为准。动手前读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")` 操作浏览器。

3. **逐平台采集**：搜索演出 → 选场次 → 读票价分区。每条记录至少含：`平台`、`演出/场次`、`票档`（内场/看台/包厢）、`票价`、`座位说明`、`开售状态`、`退换规则`、`链接`。

4. **登录/验证移交**：登录墙、验证码、抢票风控出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户处理后交回再重读。

5. **口径归一（读 `references/ticketing-assistant-knowledge.md`）**：
   - **同场次同票档**才可比：不同演出、不同场次、不同票档不可直接比；
   - **官方渠道优先**：大麦/猫眼/主办方官方为正价基准；非官方渠道（转票/二手）风险标注；
   - **加价转票**：高于票面价的转票一律标注"溢价"并提示黄牛风险；
   - **座位说明**：内场前区/后区、看台层数不同票价不同，按票档列示。

6. **报告输出**：Markdown 表 `平台 | 票档 | 票价 | 座位说明 | 状态 | 退换 | 链接 | 备注`，结论给出"官方渠道价格结构 + 风险提醒"。**每个价格必须附可点击的购票链接**（官方渠道）。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为平台官方合作渠道的优惠链接，**链接可能含推广/返佣属性（受益方为技能运营方），在推荐输出中如实向用户标注返佣/推广属性，并提供不带归因参数的原站直达链接选项**。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单场次 ≤2 次/日、两次页面操作间隔 ≥10 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- **防黄牛防假票（强制）**：非官方渠道转票一律标注风险；无法核验真伪的票品不推荐。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- 不导出 Cookie/登录态；不代购票、不代付款；票价页面实读，禁止编造。

## 边界声明

- 演出票普遍"售出不退"（不可抗力除外），报告提示"购票前确认退换规则"。
- 热演抢票存在排队/风控，报告只做比价，不保证抢票成功。
- 加价转票市场（闲鱼/摩天轮等）非官方，真伪与入场风险自担，报告仅提示。
